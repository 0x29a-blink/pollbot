# Implementation Plans

Generated by the improve skill on 2026-07-09 at commit `e720b35`. Execute in
the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row
when done.

Origin: a standard-effort audit focused on (1) the server/member-count
discrepancy between `/stats` and the admin dashboard, (2) dashboard visual
quality, (3) feature direction. Findings were verified against the code
before planning.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | [Stats accuracy: /stats vs dashboard counts](001-stats-accuracy.md) | P1 | M | — | TODO |
| 002 | [PollCard shows real poll settings](002-pollcard-settings-accuracy.md) | P1 | S | — | TODO |
| 003 | [Dashboard design foundation (tokens, font, dead config)](003-design-foundation.md) | P1 | M | — | TODO |
| 004 | [Shared UI primitives + polish (Modal/Toast/EmptyState/a11y)](004-ui-primitives-polish.md) | P2 | L | 003 | TODO |
| 005 | [Poll auto-close (scheduling Phase 1)](005-poll-auto-close.md) | P2 | M-L | 001 (shardUtils only) | TODO |
| 006 | [My Votes page](006-my-votes-page.md) | P2 | M | — (004 nice-to-have) | TODO |
| 007 | [Dashboard-vs-commands usage analytics](007-usage-source-analytics.md) | P3 | M | — | TODO |
| 008 | [Premium vote analytics](008-premium-vote-analytics.md) | P3 | M-L | — | TODO |
| 009 | [Duplicate poll + share-card spike](009-duplicate-poll-and-share-cards.md) | P3 | S-M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **004 requires 003**: the primitives consume the `@theme` tokens 003 creates.
- **005 uses 001's `src/lib/shardUtils.ts`**: if 001 hasn't run, 005 creates that file itself (instructions included).
- **001 and 007 both add migrations**: migration numbers are claimed in execution order (001 → `19`, 007 → next free, 008 → next free). Executors must check `ls supabase/migrations` before numbering.
- **Migrations are applied manually** by the maintainer in the Supabase SQL editor (this repo does not use the Supabase CLI migration history). Plans 001, 007, 008 each note this in their completion reports.
- 002 should land before 004 so the primitives migration doesn't carry the settings bug forward.
- If 005 lands before 009, the duplicate flow must not copy `ends_at` (already specified in 009).
- 007's `usage_events` can later track 009's duplicate action (`poll_duplicate`) — one line, noted in 009.

## Findings considered and rejected / deferred (do not re-audit)

- **`get_total_members` BIGINT-as-string coercion** — folded into plan 001 Step 8, not a separate plan.
- **Realtime refetch storms** (`Home.tsx:66-72` refetches everything on every vote; `UserServerView.tsx` subscribes to all votes server-wide) — real, MED effort; deferred so the redesign plans stay focused. Candidate for a future plan: debounce + guild-filtered subscriptions + patch-from-payload.
- **Unbounded client-side aggregation** (`Home.tsx:110` all polls; `ServerView.tsx:46`/`PollsView.tsx:73` raw votes tallied in JS) — real scaling ceiling; deferred. Fix direction: aggregate RPCs (pattern exists: `get_guild_vote_counts`).
- **God-component decomposition** (`UserServerView.tsx` 1034 lines, `Home.tsx` 856) — deferred; plans 004/008 work around it deliberately rather than splitting files mid-redesign.
- **framer-motion trimming** (imported in ~13 files mostly for one-shot fades a CSS class already covers) — bundle impact unmeasured; not worth a plan until measured. The one load-bearing use (AnimatePresence accordions/modals) stays regardless.
- **Two PollCard components** (`components/PollCard.tsx` vs the inline card in `UserServerView.tsx`) — unification deferred; 002 fixes the correctness half.
- **`/stats` peak-update race** — folded into plan 001 Step 6.
- **Landing.tsx localStorage auth check** (contradicts cookie sessions; likely dead) — LOW confidence it's fully dead; investigate when touching Landing, not planned.
- **More locales** (only en + es-ES ship while Top.gg copy markets multi-language) — content/translation work, not an engineering plan; maintainer's call.
- **Enabling the `GuildMembers` intent for live member counts** — rejected as a plan: privileged intent, Dev Portal + verification implications; product decision. Plan 001's hourly re-sync is the engineering answer.
- **Chart hex hardcodes** (`charts/*.tsx` tooltip/series colors) — cosmetic; roll into any future chart work (007/008 add charts that should use tokens from day one).
